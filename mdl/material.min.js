(function() {
  'use strict';

  // Инициализируем Material Design Lite
  window.addEventListener('load', function() {
    // Используем атрибут `data-upgraded` чтобы избежать повторной
    // инициализации компонентов после обновления страницы
    componentHandler.upgradeAllRegistered();
  });

})();
